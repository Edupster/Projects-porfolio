#Declare a variable that will contain a string
hero='$$$Superman$$$'
#Use the .strip() method to remove the '$' special character from the string declared and print out the result
print(hero.strip('$'))
